"""Generate markdown report from test results."""
import json
from pathlib import Path

RESULTS_DIR = Path.home() / "coalition" / "results"

def main():
    runs = []
    for f in RESULTS_DIR.glob("*.json"):
        with open(f) as fp:
            runs.append(json.load(fp))
    
    if not runs:
        print("No test results found.")
        return
    
    # Sort by score descending, then model name
    runs.sort(key=lambda x: (-x.get("summary", {}).get("passed", 0), x["model"]))
    
    system = runs[0].get("system", {})
    
    print("# Kintsugi Local Model Orchestrator Test Results")
    print()
    print("## Test Environment")
    print(f"- **Host**: {system.get(\"hostname\", \"Unknown\")}")
    print(f"- **CPU**: {system.get(\"cpu\", \"Unknown\")}")
    print(f"- **RAM**: {system.get(\"ram_gb\", \"Unknown\")} GB")
    print("- **GPUs**:")
    for gpu in system.get("gpus", []):
        print(f"  - {gpu}")
    print(f"- **Test Date**: {system.get(\"timestamp\", \"Unknown\")[:10]}")
    print()
    print("## Summary")
    print()
    print("| Model | Score | Avg Latency (ms) | Status |")
    print("|-------|-------|------------------|--------|")
    
    for run in runs:
        summary = run.get("summary", {})
        passed = summary.get("passed", 0)
        total = summary.get("total", 0)
        score = f"{passed}/{total}"
        latency = f"{summary.get(\"avg_latency_ms\", 0):.0f}"
        status = "✓ Recommended" if passed == total else "⚠ Partial"
        print(f"| {run[\"model\"]} | {score} | {latency} | {status} |")
    
    print()
    print("## Detailed Results")
    print()
    
    for run in runs:
        print(f"### {run[\"model\"]}")
        print()
        for result in run.get("results", []):
            status = "✓" if result.get("passed") else "✗"
            print(f"- {status} **{result.get(\"test\", \"Unknown\")}**: {result.get(\"latency_ms\", 0)}ms")
            if result.get("notes"):
                print(f"  - {result[\"notes\"]}")
        print()
    
    print("---")
    print("*Generated by Kintsugi Test Harness*")

if __name__ == "__main__":
    main()
